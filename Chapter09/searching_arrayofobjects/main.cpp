#include <iostream>
#include <string>
#include "student.h"
using namespace std;

int main()
{
    // How do we call the constructor to initialize each value in the array of students?
    Student studs[NUM_STUDENTS];
    return 0;
}
